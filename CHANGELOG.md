# 版本更新日志

## 1.0.3

- 修复内购商品重复计数：同一档位中的 id 序号与 productId 真 ID 不再被当成两个商品。
- 显式 productId / productID / product_id 优先，同记录的通用 id 不参与商品数量与一致性检查；无需修改项目字段名或类型名。
- 商品字段按记录边界识别，避免相邻记录、嵌套调用、注释和字符串相互干扰。
- 动态显式商品 ID 保留无法确认，不回退使用档位序号；继续检测真实额外商品和大写 ID。
- 补充七档 MemoProductTier 及商品身份优先级的正反例回归测试。

## 1.0.2

- 新增 CODE-001：A 面有效代码行数必须严格超过 5000 行；5000 行失败，5001 行通过。
- 只统计指定 A 面范围的 Swift、Objective-C、C/C++ 源码，排除空行、纯注释、测试、第三方、B 面、生成目录与构建产物。
- 新增 a_side_source_paths、code_line_excluded_paths、code_line_threshold 策略；不明确的范围和不完整统计保留需复核。
- 支持字符串中的注释符号、Swift 嵌套注释及多行/raw 字符串；重叠路径按文件去重。
- PDF 新增行数检查结论，JSON/Markdown 保留按文件计数证据。
- 补充阈值边界、词法识别、排除范围、错误处理和报告集成回归测试。

## 1.0.1

- 修复 Restore 关键词误报：协议、帮助说明及嵌入源码的法律文本不再被直接视为恢复购买功能。
- 区分说明文案与实际恢复购买代码、按钮及本地化入口；同文件存在真实功能时仍报告失败。
- 补充 StoreKit 2 `AppStore.sync()` 恢复购买证据识别。
- LaunchScreen 支持新版 Xcode `PBXFileSystemSynchronizedRootGroup`，结合构建配置、磁盘文件、target 关联及排除名单判定接入。
- 不再因为同步工程没有逐文件引用或 Resources 列表为空而要求人工复核；无法解析的路径、配置归属和排除证据仍保留具体复核原因。
- 兼容传统资源引用与同步目录混用的工程，支持本地化资源组和多项目同名启动页；阻止无关工程的文件名命中绕过排除检查。
- 启动页名称含未解析构建变量时明确保留需复核，不再猜测为 LaunchScreen。
- 新增正反例回归测试，覆盖否定声明与真实调用共存、同步目录未关联 target、资源排除及配置归属。
- 优化前代码已保存在 GitHub `backup/pre-1.0.1` 分支；本次不修改被审计的 iOS 项目。

## 1.0.0

- 首次发布 iOS A 面只读审核技能，默认交付完整中文 A4 PDF 清单。
- 内购档位按价格从 $0.99 起递增判断，不再要求命名序号。
- 应用描述非空即通过商店描述检查。
- 每次执行前检查 GitHub 最新正式 Release，支持版本校验、失败回滚和旧版本备份。

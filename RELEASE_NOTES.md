# 1.0.5

- 新增 LEGAL-001：固定检查隐私协议和用户协议，追踪协议入口、实际 URL 与应用内 WKWebView 加载关联。
- 同一协议的多个入口分别检查；外部浏览器、SFSafariViewController、本地 HTML/文件不满足 URL 加载要求；无法关联的动态路径保留需复核。
- 新增 LEGAL-002：默认联网 GET 检查实际协议 URL，同链接只请求一次，记录检查时间、最终 URL、HTTP 状态和响应类型。
- 明确失效链接、空页面与重定向循环报告失败；网络受阻、验证页面和 JS 空壳保留具体复核原因，不把 HTTP 200 一律当作成功。
- 限制请求超时、重定向及读取大小，校验 TLS 与每跳公网地址，不携带 Cookie 或认证信息。
- PDF 新增两项完整清单，JSON/Markdown 保留入口与联网明细，schema 2.0 保持兼容。
- 补充源码关联、联网响应、去重、只读边界及 PDF 回归测试；目标 iOS 项目仍只读，不构建或运行。

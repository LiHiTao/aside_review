# 1.0.11

- 修复 .init(...) 和 Type.init(...) 被误当初始化声明而跳过商品字段的问题，补齐显式类型数组中的简写初始化识别。
- 保守关联静态商品目录引用，区分商品定义与交易结果读取，保留未知动态购买来源的需复核判定。
- 保留显式 productID 优先、普通业务 ID 排除和代码/JSON 不一致检查；新增正反回归，保持命令行、schema 2.0 与 17 项报告兼容。
